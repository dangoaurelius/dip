import { createAction } from 'redux-act';

export const setSchedule = createAction('Set Schedule');
export const editSchedule = createAction('Edit Schedule');
export const removeSchedule = createAction('Remove Schedule');
